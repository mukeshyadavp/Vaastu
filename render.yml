services:
  - type: web
    name: vaastu-fullstack
    env: python
    plan: free
    region: oregon

    buildCommand: |
      pip install -r backend/requirements.txt
      npm install
      npm run build

    startCommand: |
      cd backend && gunicorn -w 4 -b 0.0.0.0:$PORT app:app